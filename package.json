{
  "name": "auto-elite-root",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npx -y concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npx -y concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\""
  }
}
